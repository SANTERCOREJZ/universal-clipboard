// Project-level build file. Declares which Gradle plugins exist — does NOT apply them here.
// Each sub-project (like :app) applies the plugins it needs in its own build.gradle.kts.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
